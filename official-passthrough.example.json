{
  "enabled": true,
  "fallbackCodexAuthJson": true,
  "codexAuthPath": null,
  "modelPatterns": [
    "gpt-*",
    "codex-*"
  ],
  "_comment": "Codex official passthrough. Gate: Codex client only; if model present and outside modelPatterns → 9router route; else passthrough. Path is not a gate. modelPatterns: bare globs (gpt-*, codex-*). Prefixed ids (cx/gpt-*) never match."
}
